{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Slack is a single-page application with registration and authentication. All user actions are tracked and processed in real-time.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ArthurFloyd",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.3"
  }
}